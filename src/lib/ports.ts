/** Default host port for the collector + dashboard (install / production). */
export const DEFAULT_COLLECTOR_PORT = 31337;

export const defaultCollectorUrl = `http://127.0.0.1:${DEFAULT_COLLECTOR_PORT}`;
