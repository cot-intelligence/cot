/** Default host port for the collector + dashboard (install / production). */
export const DEFAULT_COLLECTOR_PORT = 8000;

export const defaultCollectorUrl = `http://localhost:${DEFAULT_COLLECTOR_PORT}`;
